<%= simple_form_for([@user_review, @user_review.reviews.build]) do |f| %>

<div id="rating-form">
		<label>Note :</label>
	</div>
	<br>
	<%= f.input :comment %>
	<%= f.button :submit, :class => "btn-custom2" %>
<% end %>
